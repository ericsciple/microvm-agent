# scripts/

Host-side provisioning scripts, called from `src/main.js`. Port the proven bash
from `github/ericsciple-planning/.github/workflows/agent-sandbox-phase{0..6}-*.yml`
here (KVM setup, tap/NAT, firewall, gateway, rootfs build, mounts). See ../TODO.md.
