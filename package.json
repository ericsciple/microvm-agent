{
  "name": "microvm-agent",
  "version": "0.0.0",
  "description": "Safely run an AI agent prompt inside a Firecracker microVM as a GitHub Action.",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step: zero-dependency ESM action; runs.main points directly at src/main.js.'",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": false
}
